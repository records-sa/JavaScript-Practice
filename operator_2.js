// 제비뽑기 용지

function solution(a, b) {
  const 종이 = a * b;
  return 가위질 = 종이 - 1;
}

solution(100, 100);


// 분기별 일정

function solution(month) {
  const 분기 = month / 3;
  return Math.ceil(분기);
}

solution(4);
solution(12);